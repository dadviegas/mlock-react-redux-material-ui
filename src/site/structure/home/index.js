import { route } from "../../../js/route"
import {  } from "../../../js/route"

export const home = route({
  icon: 'home',
  name: 'Default',
  description: 'Default',
  path: '/',
  payload: {
    markdownFile: 'default.md'
  }
})
